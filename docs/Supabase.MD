"
create table logs (
  id bigint generated by default as identity primary key,
  created_at timestamp with time zone default timezone('utc'::text, now()) not null,
  date_key text unique not null,  -- Stores "Fri Jan 30 2026"
  am_in text,
  am_out text,
  pm_in text,
  pm_out text,
  activity text,
  accomplished text
);

"